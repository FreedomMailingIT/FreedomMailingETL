"""
Test transform program ability handle badly scructured file names
"""


def test_compression(bad_text_fname='', clean_zipped_fname=''):
    """Compress badly named text file & compress with corrected filename"""
    _ = bad_text_fname
    _ = clean_zipped_fname
    assert True is False


def test_bad_zip_name(bad_zip_fname=''):
    """Handle badly named zip filename"""
    _ = bad_zip_fname
    assert True is False
